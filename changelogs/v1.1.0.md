### New Features
- **Multi-video post support**: Posts with multiple videos now show a chip selector in the info card.

### Bug Fixes
- Tapping the trim slider track now seeks the player instead of snapping a trim handle
- Preview loading bar now follows the app accent color
- Rename field outline stroke and text cursor now follow accent color
- Tapping outside the rename field now dismisses the keyboard and clears the selection
- Downloading the same URL more than once no longer overwrites the existing file.